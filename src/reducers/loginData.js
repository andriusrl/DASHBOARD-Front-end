const initialState = {
    loginData: undefined
}

const LoginData = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default LoginData;